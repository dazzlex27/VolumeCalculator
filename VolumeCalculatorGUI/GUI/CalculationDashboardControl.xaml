<UserControl x:Class="VolumeCalculatorGUI.GUI.CalculationDashboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:gui="clr-namespace:VolumeCalculatorGUI.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" x:ClassModifier="internal"
             d:DataContext="{d:DesignInstance gui:CalculationDashboardControlVm}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<StackPanel Grid.Column="0" Name="SpCounters" Margin="5" >
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="Длина = "/>
				<TextBlock Text="{Binding Path=ObjLength}"/>
				<TextBlock Text="  мм"/>
			</StackPanel>
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="Ширина = "/>
				<TextBlock Text="{Binding Path=ObjWidth}"/>
				<TextBlock Text="  мм"/>
			</StackPanel>
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="Высота = "/>
				<TextBlock Text="{Binding Path=ObjHeight}"/>
				<TextBlock Text="  мм"/>
			</StackPanel>
			<StackPanel Orientation="Horizontal">
				<TextBlock Text="Объем = "/>
				<TextBlock Text="{Binding Path=ObjVolume}"/>
				<TextBlock Text="  мм"/>
			</StackPanel>
		</StackPanel>
		<Button Grid.Column="0" 
		        Grid.Row="1" 
		        IsEnabled="False" 
		        Margin="2" 
		        MinHeight="25" 
		        Name="BtCalcVolume"
		        ToolTipService.ShowOnDisabled="{Binding Path=IsEnabled, ElementName=BtCalcVolume, Converter={StaticResource InverseBooleanConverter}}" 
		        ToolTip="Нет данных для вычисления" 
		        Content="Выполнить измерение" 
		        Command="{Binding CalculateVolumeCommand}"/>
	</Grid>
</UserControl>