<Window x:Class="VolumeCheckerGUI.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:gui="clr-namespace:VolumeCheckerGUI.GUI"
        mc:Ignorable="d"
        Title="VolumeCheckerGUI" SizeToContent="WidthAndHeight" MinHeight="250" MinWidth="600" WindowStartupLocation="CenterScreen" 
		ResizeMode="CanMinimize" Closing="Window_Closing" ContentRendered="Window_ContentRendered">
	<Window.DataContext>
		<gui:MainWindowVm/>
	</Window.DataContext>
	<StackPanel>
		<Menu>
			<MenuItem Header="Действия">
				<MenuItem Name="MiOpenSettings" Header="Изменить настройки..." Click="MiOpenSettings_Click"/>
				<MenuItem Name="MiExitApp" Header="Выйти из приложения" Click="MiExitApp_Click"/>
			</MenuItem>
		</Menu>
		<Grid Margin="5">
			<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition/>
			</Grid.ColumnDefinitions>
			<StackPanel Margin="5" Grid.Row="0" Width="Auto" HorizontalAlignment="Left">
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition/>
						<ColumnDefinition/>
					</Grid.ColumnDefinitions>
					<Canvas Grid.Column="0" Width="400" Height="300" Background="Black" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
						<Image Source="{Binding ColorImageBitmap}" Name="ImgColor" Width="400" Height="300" />
					</Canvas>
					<Canvas Margin="5" Grid.Column="1" Width="400" Height="300" Background="Black">
						<Image Source="{Binding DepthImageBitmap}" Name="ImgDm" Width="400" Height="300" />
					</Canvas>
				</Grid>
				<Button IsEnabled="False" Margin="2" MinHeight="25" ToolTipService.ShowOnDisabled="True" ToolTip="Нет данных для вычисления" 
				        Name="BtMeasureVolume" Content="Выполнить измерение" Click="BtMeasureVolume_Click"/>
			</StackPanel>
			<StackPanel Margin="5" Grid.Column="1">
				<StackPanel Orientation="Horizontal">
					<TextBlock Text="W = "/>
					<TextBlock Text="{Binding Path=ObjWidth}"/>
					<TextBlock Text="  mm"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<TextBlock Text="H = "/>
					<TextBlock Text="{Binding Path=ObjHeight}"/>
					<TextBlock Text="  mm"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<TextBlock Text="D = "/>
					<TextBlock Text="{Binding Path=ObjDepth}"/>
					<TextBlock Text="  mm"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<TextBlock Text="V = "/>
					<TextBlock Text="{Binding Path=ObjVolume}"/>
					<TextBlock Text="  mm"/>
				</StackPanel>
			</StackPanel>
		</Grid>
	</StackPanel>
</Window>
