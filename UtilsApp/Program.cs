using System.IO;
using Common;

namespace UtilsApp
{
	class Program
	{
		static void Main(string[] args)
		{
			//const string inDirectoryName = "dms";
			//const string outDirectoryName = "out_dms";

			//var dirInfo = new DirectoryInfo(inDirectoryName);
			//Directory.CreateDirectory(outDirectoryName);

			//foreach (var file in dirInfo.EnumerateFiles())
			//{
			//	var dm = DepthMapUtils.ReadDepthMapFromFile(file.FullName);

			//	var bitmap = DepthMapUtils.CreateBitmapFromDepthMap(dm, 600, 3500, 3500);

			//	var filenameNoExt = file.Name.Substring(0, file.Name.LastIndexOf('.'));

			//	bitmap.Save(Path.Combine(outDirectoryName, $"{filenameNoExt}.png"));
			//}

		}
	}
}
